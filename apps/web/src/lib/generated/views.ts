// Auto-generated by cospan-codegen via panproto protolens (from JSON lens files).
// Source: packages/lenses/*.lens.json
// Do not edit manually.

// dev.cospan.repo.pull (via lens file)
export interface PullView {
  did: string;
  rkey: string;
  body: string | null;
  createdAt: string;
  sourceRef: string;
  sourceRepo: string | null;
  targetRef: string;
  title: string;
  commentCount: number;
  repoDid: string;
  repoName: string;
  state: string;
  indexedAt: string;
}

export function normalizePullView(raw: Partial<PullView>): PullView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    body: raw.body ?? null,
    createdAt: raw.createdAt ?? '',
    sourceRef: raw.sourceRef ?? '',
    sourceRepo: raw.sourceRepo ?? null,
    targetRef: raw.targetRef ?? '',
    title: raw.title ?? '',
    commentCount: raw.commentCount ?? 0,
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    state: raw.state ?? 'open',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.issue.state (via lens file)
export interface IssueStateView {
  did: string;
  rkey: string;
  createdAt: string;
  issueUri: string | null;
  reason: string | null;
  state: string;
  indexedAt: string;
}

export function normalizeIssueStateView(raw: Partial<IssueStateView>): IssueStateView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    issueUri: raw.issueUri ?? null,
    reason: raw.reason ?? null,
    state: raw.state ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.issue (via lens file)
export interface IssueView {
  did: string;
  rkey: string;
  body: string | null;
  createdAt: string;
  title: string;
  commentCount: number;
  repoDid: string;
  repoName: string;
  state: string;
  indexedAt: string;
}

export function normalizeIssueView(raw: Partial<IssueView>): IssueView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    body: raw.body ?? null,
    createdAt: raw.createdAt ?? '',
    title: raw.title ?? '',
    commentCount: raw.commentCount ?? 0,
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    state: raw.state ?? 'open',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.issue.comment (via lens file)
export interface IssueCommentView {
  did: string;
  rkey: string;
  body: string;
  createdAt: string;
  issueUri: string | null;
  indexedAt: string;
}

export function normalizeIssueCommentView(raw: Partial<IssueCommentView>): IssueCommentView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    body: raw.body ?? '',
    createdAt: raw.createdAt ?? '',
    issueUri: raw.issueUri ?? null,
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.org (via lens file)
export interface OrgView {
  did: string;
  rkey: string;
  createdAt: string;
  description: string | null;
  name: string;
  avatarCid: string;
  indexedAt: string;
}

export function normalizeOrgView(raw: Partial<OrgView>): OrgView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    description: raw.description ?? null,
    name: raw.name ?? '',
    avatarCid: raw.avatarCid ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo (via lens file)
export interface RepoView {
  did: string;
  rkey: string;
  createdAt: string;
  defaultBranch: string | null;
  description: string | null;
  name: string;
  protocol: string;
  sourceRepo: string | null;
  visibility: string | null;
  forkCount: number;
  nodeDid: string;
  nodeUrl: string;
  openIssueCount: number;
  openMrCount: number;
  source: string;
  sourceUri: string;
  starCount: number;
  indexedAt: string;
}

export function normalizeRepoView(raw: Partial<RepoView>): RepoView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    defaultBranch: raw.defaultBranch ?? null,
    description: raw.description ?? null,
    name: raw.name ?? '',
    protocol: raw.protocol ?? '',
    sourceRepo: raw.sourceRepo ?? null,
    visibility: raw.visibility ?? null,
    forkCount: raw.forkCount ?? 0,
    nodeDid: raw.nodeDid ?? '',
    nodeUrl: raw.nodeUrl ?? '',
    openIssueCount: raw.openIssueCount ?? 0,
    openMrCount: raw.openMrCount ?? 0,
    source: raw.source ?? '',
    sourceUri: raw.sourceUri ?? '',
    starCount: raw.starCount ?? 0,
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.label.definition (via lens file)
export interface LabelView {
  did: string;
  rkey: string;
  color: string;
  createdAt: string;
  description: string | null;
  name: string;
  repoDid: string;
  repoName: string;
  indexedAt: string;
}

export function normalizeLabelView(raw: Partial<LabelView>): LabelView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    color: raw.color ?? '',
    createdAt: raw.createdAt ?? '',
    description: raw.description ?? null,
    name: raw.name ?? '',
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.org.member (via lens file)
export interface OrgMemberView {
  did: string;
  rkey: string;
  createdAt: string;
  memberDid: string | null;
  orgUri: string | null;
  role: string;
  indexedAt: string;
}

export function normalizeOrgMemberView(raw: Partial<OrgMemberView>): OrgMemberView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    memberDid: raw.memberDid ?? null,
    orgUri: raw.orgUri ?? null,
    role: raw.role ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.actor.profile (via lens file)
export interface ActorProfileView {
  did: string;
  bluesky: string;
  description: string | null;
  displayName: string | null;
  avatarCid: string;
  indexedAt: string;
}

export function normalizeActorProfileView(raw: Partial<ActorProfileView>): ActorProfileView {
  return {
    did: raw.did ?? '',
    bluesky: raw.bluesky ?? '',
    description: raw.description ?? null,
    displayName: raw.displayName ?? null,
    avatarCid: raw.avatarCid ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.collaborator (via lens file)
export interface CollaboratorView {
  did: string;
  rkey: string;
  createdAt: string;
  did: string;
  role: string;
  repoDid: string;
  repoName: string;
  indexedAt: string;
}

export function normalizeCollaboratorView(raw: Partial<CollaboratorView>): CollaboratorView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    did: raw.did ?? '',
    role: raw.role ?? '',
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.dependency (via lens file)
export interface DependencyView {
  did: string;
  rkey: string;
  createdAt: string;
  description: string | null;
  morphismId: string;
  sourceProtocol: string | null;
  targetProtocol: string | null;
  sourceRepoDid: string;
  sourceRepoName: string;
  targetRepoDid: string;
  targetRepoName: string;
  indexedAt: string;
}

export function normalizeDependencyView(raw: Partial<DependencyView>): DependencyView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    description: raw.description ?? null,
    morphismId: raw.morphismId ?? '',
    sourceProtocol: raw.sourceProtocol ?? null,
    targetProtocol: raw.targetProtocol ?? null,
    sourceRepoDid: raw.sourceRepoDid ?? '',
    sourceRepoName: raw.sourceRepoName ?? '',
    targetRepoDid: raw.targetRepoDid ?? '',
    targetRepoName: raw.targetRepoName ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.vcs.refUpdate (via lens file)
export interface RefUpdateView {
  rkey: string;
  commitCount: number | null;
  committerDid: string;
  createdAt: string;
  lensId: string | null;
  lensQuality: Record<string, unknown> | null;
  migrationId: string | null;
  newTarget: string;
  oldTarget: string | null;
  protocol: string;
  ref: string;
  breakingChangeCount: number;
  repoDid: string;
  repoName: string;
  indexedAt: string;
}

export function normalizeRefUpdateView(raw: Partial<RefUpdateView>): RefUpdateView {
  return {
    rkey: raw.rkey ?? '',
    commitCount: raw.commitCount ?? null,
    committerDid: raw.committerDid ?? '',
    createdAt: raw.createdAt ?? '',
    lensId: raw.lensId ?? null,
    lensQuality: raw.lensQuality ?? null,
    migrationId: raw.migrationId ?? null,
    newTarget: raw.newTarget ?? '',
    oldTarget: raw.oldTarget ?? null,
    protocol: raw.protocol ?? '',
    ref: raw.ref ?? '',
    breakingChangeCount: raw.breakingChangeCount ?? 0,
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.pull.state (via lens file)
export interface PullStateView {
  did: string;
  rkey: string;
  createdAt: string;
  mergeCommitId: string | null;
  pullUri: string | null;
  state: string;
  indexedAt: string;
}

export function normalizePullStateView(raw: Partial<PullStateView>): PullStateView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    mergeCommitId: raw.mergeCommitId ?? null,
    pullUri: raw.pullUri ?? null,
    state: raw.state ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.pull.comment (via lens file)
export interface PullCommentView {
  did: string;
  rkey: string;
  body: string;
  createdAt: string;
  pullUri: string | null;
  reviewDecision: string | null;
  indexedAt: string;
}

export function normalizePullCommentView(raw: Partial<PullCommentView>): PullCommentView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    body: raw.body ?? '',
    createdAt: raw.createdAt ?? '',
    pullUri: raw.pullUri ?? null,
    reviewDecision: raw.reviewDecision ?? null,
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.pipeline (via lens file)
export interface PipelineView {
  did: string;
  rkey: string;
  commitId: string;
  completedAt: string | null;
  createdAt: string;
  ref: string | null;
  status: string;
  workflows: unknown[] | null;
  breakingChangeCheck: string;
  equationVerification: string;
  gatTypeCheck: string;
  lensLawCheck: string;
  repoDid: string;
  repoName: string;
  indexedAt: string;
}

export function normalizePipelineView(raw: Partial<PipelineView>): PipelineView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    commitId: raw.commitId ?? '',
    completedAt: raw.completedAt ?? null,
    createdAt: raw.createdAt ?? '',
    ref: raw.ref ?? null,
    status: raw.status ?? 'pending',
    workflows: raw.workflows ?? null,
    breakingChangeCheck: raw.breakingChangeCheck ?? '',
    equationVerification: raw.equationVerification ?? '',
    gatTypeCheck: raw.gatTypeCheck ?? '',
    lensLawCheck: raw.lensLawCheck ?? '',
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

export interface RepoListResponse {
  repos: RepoView[];
  cursor: string | null;
}

export interface IssueListResponse {
  issues: IssueView[];
  cursor: string | null;
}

export interface IssueCommentListResponse {
  comments: IssueCommentView[];
  cursor: string | null;
}

export interface PullListResponse {
  pulls: PullView[];
  cursor: string | null;
}

export interface PullCommentListResponse {
  comments: PullCommentView[];
  cursor: string | null;
}

export interface StarListResponse {
  stars: StarView[];
  cursor: string | null;
}

export interface FollowListResponse {
  follows: FollowView[];
  cursor: string | null;
}

export interface NodeListResponse {
  nodes: NodeView[];
  cursor: string | null;
}

export interface OrgListResponse {
  orgs: OrgView[];
  cursor: string | null;
}

export interface OrgMemberListResponse {
  members: OrgMemberView[];
  cursor: string | null;
}

export interface CollaboratorListResponse {
  collaborators: CollaboratorView[];
  cursor: string | null;
}

export interface RefUpdateListResponse {
  refUpdates: RefUpdateView[];
  cursor: string | null;
}

export interface LabelListResponse {
  labels: LabelView[];
  cursor: string | null;
}

export interface PipelineListResponse {
  pipelines: PipelineView[];
  cursor: string | null;
}

export interface ReactionListResponse {
  reactions: ReactionView[];
  cursor: string | null;
}

