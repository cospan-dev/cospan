// Auto-generated by cospan-codegen via panproto Protolens.
// Source: Lexicon schemas transformed through DB projection lens.
// Do not edit manually.

// dev.cospan.node (via Protolens)
export interface NodeView {
  did: string;
  rkey: string;
  createdAt: string;
  publicEndpoint: string | null;
  indexedAt: string;
}

export function normalizeNodeView(raw: Partial<NodeView>): NodeView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    publicEndpoint: raw.publicEndpoint ?? null,
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.actor.profile (via Protolens)
export interface ActorProfileView {
  did: string;
  bluesky: string;
  description: string | null;
  displayName: string | null;
  avatarCid: string;
  indexedAt: string;
}

export function normalizeActorProfileView(raw: Partial<ActorProfileView>): ActorProfileView {
  return {
    did: raw.did ?? '',
    bluesky: raw.bluesky ?? '',
    description: raw.description ?? null,
    displayName: raw.displayName ?? null,
    avatarCid: raw.avatarCid ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo (via Protolens)
export interface RepoView {
  did: string;
  rkey: string;
  createdAt: string;
  description: string | null;
  name: string;
  protocol: string;
  sourceRepo: string | null;
  defaultBranch: string;
  forkCount: number;
  nodeDid: string;
  nodeUrl: string;
  openIssueCount: number;
  openMrCount: number;
  source: string;
  sourceUri: string;
  starCount: number;
  visibility: string;
  indexedAt: string;
}

export function normalizeRepoView(raw: Partial<RepoView>): RepoView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    description: raw.description ?? null,
    name: raw.name ?? '',
    protocol: raw.protocol ?? '',
    sourceRepo: raw.sourceRepo ?? null,
    defaultBranch: raw.defaultBranch ?? '',
    forkCount: raw.forkCount ?? 0,
    nodeDid: raw.nodeDid ?? '',
    nodeUrl: raw.nodeUrl ?? '',
    openIssueCount: raw.openIssueCount ?? 0,
    openMrCount: raw.openMrCount ?? 0,
    source: raw.source ?? '',
    sourceUri: raw.sourceUri ?? '',
    starCount: raw.starCount ?? 0,
    visibility: raw.visibility ?? 'public',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.vcs.refUpdate (via Protolens)
export interface RefUpdateView {
  rkey: string;
  committerDid: string;
  createdAt: string;
  lensId: string | null;
  migrationId: string | null;
  newTarget: string;
  oldTarget: string | null;
  protocol: string;
  ref: string;
  breakingChangeCount: number;
  commitCount: number;
  lensQuality: number;
  repoDid: string;
  repoName: string;
  indexedAt: string;
}

export function normalizeRefUpdateView(raw: Partial<RefUpdateView>): RefUpdateView {
  return {
    rkey: raw.rkey ?? '',
    committerDid: raw.committerDid ?? '',
    createdAt: raw.createdAt ?? '',
    lensId: raw.lensId ?? null,
    migrationId: raw.migrationId ?? null,
    newTarget: raw.newTarget ?? '',
    oldTarget: raw.oldTarget ?? null,
    protocol: raw.protocol ?? '',
    ref: raw.ref ?? '',
    breakingChangeCount: raw.breakingChangeCount ?? 0,
    commitCount: raw.commitCount ?? 0,
    lensQuality: raw.lensQuality ?? 0,
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.issue (via Protolens)
export interface IssueView {
  did: string;
  rkey: string;
  body: string | null;
  createdAt: string;
  title: string;
  commentCount: number;
  repoDid: string;
  repoName: string;
  state: string;
  indexedAt: string;
}

export function normalizeIssueView(raw: Partial<IssueView>): IssueView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    body: raw.body ?? null,
    createdAt: raw.createdAt ?? '',
    title: raw.title ?? '',
    commentCount: raw.commentCount ?? 0,
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    state: raw.state ?? 'open',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.issue.comment (via Protolens)
export interface IssueCommentView {
  did: string;
  rkey: string;
  body: string;
  createdAt: string;
  indexedAt: string;
}

export function normalizeIssueCommentView(raw: Partial<IssueCommentView>): IssueCommentView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    body: raw.body ?? '',
    createdAt: raw.createdAt ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.issue.state (via Protolens)
export interface IssueStateView {
  did: string;
  rkey: string;
  createdAt: string;
  reason: string | null;
  state: string;
  indexedAt: string;
}

export function normalizeIssueStateView(raw: Partial<IssueStateView>): IssueStateView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    reason: raw.reason ?? null,
    state: raw.state ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.pull (via Protolens)
export interface PullView {
  did: string;
  rkey: string;
  body: string | null;
  createdAt: string;
  sourceRef: string;
  sourceRepo: string | null;
  targetRef: string;
  title: string;
  commentCount: number;
  repoDid: string;
  repoName: string;
  state: string;
  indexedAt: string;
}

export function normalizePullView(raw: Partial<PullView>): PullView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    body: raw.body ?? null,
    createdAt: raw.createdAt ?? '',
    sourceRef: raw.sourceRef ?? '',
    sourceRepo: raw.sourceRepo ?? null,
    targetRef: raw.targetRef ?? '',
    title: raw.title ?? '',
    commentCount: raw.commentCount ?? 0,
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    state: raw.state ?? 'open',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.pull.comment (via Protolens)
export interface PullCommentView {
  did: string;
  rkey: string;
  body: string;
  createdAt: string;
  reviewDecision: string | null;
  indexedAt: string;
}

export function normalizePullCommentView(raw: Partial<PullCommentView>): PullCommentView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    body: raw.body ?? '',
    createdAt: raw.createdAt ?? '',
    reviewDecision: raw.reviewDecision ?? null,
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.pull.state (via Protolens)
export interface PullStateView {
  did: string;
  rkey: string;
  createdAt: string;
  mergeCommitId: string | null;
  state: string;
  indexedAt: string;
}

export function normalizePullStateView(raw: Partial<PullStateView>): PullStateView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    mergeCommitId: raw.mergeCommitId ?? null,
    state: raw.state ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.feed.star (via Protolens)
export interface StarView {
  did: string;
  rkey: string;
  createdAt: string;
  subject: string;
  indexedAt: string;
}

export function normalizeStarView(raw: Partial<StarView>): StarView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    subject: raw.subject ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.feed.reaction (via Protolens)
export interface ReactionView {
  did: string;
  rkey: string;
  createdAt: string;
  emoji: string;
  subject: string;
  indexedAt: string;
}

export function normalizeReactionView(raw: Partial<ReactionView>): ReactionView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    emoji: raw.emoji ?? '',
    subject: raw.subject ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.graph.follow (via Protolens)
export interface FollowView {
  did: string;
  rkey: string;
  createdAt: string;
  subject: string;
  indexedAt: string;
}

export function normalizeFollowView(raw: Partial<FollowView>): FollowView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    subject: raw.subject ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.label.definition (via Protolens)
export interface LabelView {
  did: string;
  rkey: string;
  color: string;
  createdAt: string;
  description: string | null;
  name: string;
  repoDid: string;
  repoName: string;
  indexedAt: string;
}

export function normalizeLabelView(raw: Partial<LabelView>): LabelView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    color: raw.color ?? '',
    createdAt: raw.createdAt ?? '',
    description: raw.description ?? null,
    name: raw.name ?? '',
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.org (via Protolens)
export interface OrgView {
  did: string;
  rkey: string;
  createdAt: string;
  description: string | null;
  name: string;
  avatarCid: string;
  indexedAt: string;
}

export function normalizeOrgView(raw: Partial<OrgView>): OrgView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    description: raw.description ?? null,
    name: raw.name ?? '',
    avatarCid: raw.avatarCid ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.org.member (via Protolens)
export interface OrgMemberView {
  did: string;
  rkey: string;
  createdAt: string;
  role: string;
  indexedAt: string;
}

export function normalizeOrgMemberView(raw: Partial<OrgMemberView>): OrgMemberView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    role: raw.role ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.collaborator (via Protolens)
export interface CollaboratorView {
  did: string;
  rkey: string;
  createdAt: string;
  role: string;
  repoDid: string;
  repoName: string;
  indexedAt: string;
}

export function normalizeCollaboratorView(raw: Partial<CollaboratorView>): CollaboratorView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    role: raw.role ?? '',
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.repo.dependency (via Protolens)
export interface DependencyView {
  did: string;
  rkey: string;
  createdAt: string;
  description: string | null;
  morphismId: string;
  sourceProtocol: string | null;
  targetProtocol: string | null;
  sourceRepoDid: string;
  sourceRepoName: string;
  targetRepoDid: string;
  targetRepoName: string;
  indexedAt: string;
}

export function normalizeDependencyView(raw: Partial<DependencyView>): DependencyView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    createdAt: raw.createdAt ?? '',
    description: raw.description ?? null,
    morphismId: raw.morphismId ?? '',
    sourceProtocol: raw.sourceProtocol ?? null,
    targetProtocol: raw.targetProtocol ?? null,
    sourceRepoDid: raw.sourceRepoDid ?? '',
    sourceRepoName: raw.sourceRepoName ?? '',
    targetRepoDid: raw.targetRepoDid ?? '',
    targetRepoName: raw.targetRepoName ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

// dev.cospan.pipeline (via Protolens)
export interface PipelineView {
  did: string;
  rkey: string;
  commitId: string;
  completedAt: string | null;
  createdAt: string;
  ref: string | null;
  status: string;
  breakingChangeCheck: string;
  equationVerification: string;
  gatTypeCheck: string;
  lensLawCheck: string;
  repoDid: string;
  repoName: string;
  indexedAt: string;
}

export function normalizePipelineView(raw: Partial<PipelineView>): PipelineView {
  return {
    did: raw.did ?? '',
    rkey: raw.rkey ?? '',
    commitId: raw.commitId ?? '',
    completedAt: raw.completedAt ?? null,
    createdAt: raw.createdAt ?? '',
    ref: raw.ref ?? null,
    status: raw.status ?? 'pending',
    breakingChangeCheck: raw.breakingChangeCheck ?? '',
    equationVerification: raw.equationVerification ?? '',
    gatTypeCheck: raw.gatTypeCheck ?? '',
    lensLawCheck: raw.lensLawCheck ?? '',
    repoDid: raw.repoDid ?? '',
    repoName: raw.repoName ?? '',
    indexedAt: raw.indexedAt ?? '',
  };
}

export interface RepoListResponse {
  repos: RepoView[];
  cursor: string | null;
}

export interface IssueListResponse {
  issues: IssueView[];
  cursor: string | null;
}

export interface IssueCommentListResponse {
  comments: IssueCommentView[];
  cursor: string | null;
}

export interface PullListResponse {
  pulls: PullView[];
  cursor: string | null;
}

export interface PullCommentListResponse {
  comments: PullCommentView[];
  cursor: string | null;
}

export interface StarListResponse {
  stars: StarView[];
  cursor: string | null;
}

export interface FollowListResponse {
  follows: FollowView[];
  cursor: string | null;
}

export interface NodeListResponse {
  nodes: NodeView[];
  cursor: string | null;
}

export interface OrgListResponse {
  orgs: OrgView[];
  cursor: string | null;
}

export interface OrgMemberListResponse {
  members: OrgMemberView[];
  cursor: string | null;
}

export interface CollaboratorListResponse {
  collaborators: CollaboratorView[];
  cursor: string | null;
}

export interface RefUpdateListResponse {
  refUpdates: RefUpdateView[];
  cursor: string | null;
}

export interface LabelListResponse {
  labels: LabelView[];
  cursor: string | null;
}

export interface PipelineListResponse {
  pipelines: PipelineView[];
  cursor: string | null;
}

export interface ReactionListResponse {
  reactions: ReactionView[];
  cursor: string | null;
}

