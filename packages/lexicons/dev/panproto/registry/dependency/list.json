{
  "lexicon": 1,
  "id": "dev.panproto.registry.dependency.list",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["did", "repo"],
        "properties": {
          "did": { "type": "string", "format": "did" },
          "repo": { "type": "string" },
          "direction": { "type": "string", "knownValues": ["dependencies", "dependents"] },
          "limit": { "type": "integer", "maximum": 100 },
          "cursor": { "type": "string" }
        }
      }
    }
  }
}
