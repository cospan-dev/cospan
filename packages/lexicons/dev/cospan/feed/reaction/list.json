{
  "lexicon": 1,
  "id": "dev.cospan.feed.reaction.list",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["subject"],
        "properties": {
          "subject": { "type": "string", "format": "at-uri" },
          "limit": { "type": "integer", "maximum": 100 },
          "cursor": { "type": "string" }
        }
      }
    }
  }
}
