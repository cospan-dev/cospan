{
  "lexicon": 1,
  "id": "dev.cospan.vcs.refUpdate.list",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["did", "repo"],
        "properties": {
          "did": { "type": "string", "format": "did" },
          "repo": { "type": "string" },
          "limit": { "type": "integer", "maximum": 100 },
          "cursor": { "type": "integer" }
        }
      }
    }
  }
}
