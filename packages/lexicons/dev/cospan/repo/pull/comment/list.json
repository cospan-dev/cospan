{
  "lexicon": 1,
  "id": "dev.cospan.repo.pull.comment.list",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["pull"],
        "properties": {
          "pull": { "type": "string", "format": "at-uri" },
          "limit": { "type": "integer", "maximum": 100 },
          "cursor": { "type": "string" }
        }
      }
    }
  }
}
