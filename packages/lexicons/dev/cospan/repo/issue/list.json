{
  "lexicon": 1,
  "id": "dev.cospan.repo.issue.list",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["did", "repo"],
        "properties": {
          "did": { "type": "string", "format": "did" },
          "repo": { "type": "string" },
          "state": { "type": "string" },
          "limit": { "type": "integer", "maximum": 100 },
          "cursor": { "type": "string" }
        }
      }
    }
  }
}
