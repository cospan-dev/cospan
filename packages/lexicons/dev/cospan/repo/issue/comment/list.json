{
  "lexicon": 1,
  "id": "dev.cospan.repo.issue.comment.list",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["issue"],
        "properties": {
          "issue": { "type": "string", "format": "at-uri" },
          "limit": { "type": "integer", "maximum": 100 },
          "cursor": { "type": "string" }
        }
      }
    }
  }
}
